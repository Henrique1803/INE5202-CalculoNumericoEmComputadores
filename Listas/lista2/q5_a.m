%Para localizar um intervalo [𝑎,𝑏] onde haja um zero positivo de
%f(x), podemos observar o comportamento da função e determinar onde ela
% cruza o eixo x (ou seja, onde f(x)=0 positivamente).

% Vamos examinar o comportamento de f(x) entre
% x=2 e x=3, já que esse é o intervalo fornecido:

% f(2) = 2⋅cos(2)+1.9 ≈ −0.114
% f(3) = 3⋅cos(3)+1.9 ≈ 0.08

% Como f(2) é negativo e f(3) é positivo, isso sugere que
% há pelo menos um zero positivo de f(x) no intervalo [2,3] (teorema de Bolzano).

%Seja f (x) uma função contínua
%num intervalo [a, b]. Se f (a).f (b)<0, então
%existe pelo menos um zero de f (x) entre a e b.
